in ram, 0xC000:
    package stack
        let SIZE = 256
        var stack : byte * SIZE
        let top = stack + SIZE
    end

    package hardware
        let GB = 0
        let GBC = 1
        let GBA = 2
        var type : byte
    end

    package video
        package request
            var redraw : byte
        end
    end

    var scroll_direction : byte